<!DOCTYPE HTML5>
<html>
	<head>
		<title>Break the Egg</title>
		<!--		<link rel="stylesheet" href="{{url_for('static', filename='css/basic.css')}}">-->
		<link rel="shorcut icon" href="{{url_for('static', filename='img/favicon.ico')}}">
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
		<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
	</head>

	<body>
		<p>
			<div class="inset"><i>EggBreaker:</i>
				<a href="{{url_for('home_page')}}">Home</a>
				<a href="{{ url_for('my_login') }}">Login</a>
				{% if current_user.is_authenticated %}
				<a href="{{  url_for('my_logout')  }}">Logout</a>
				<a href="{{ url_for('profile')}}">Profile</a>
				{% endif %}
				<a href="{{ url_for('game') }}">Stats</a>
			</div>
	  </p>
		<h1>What is inside the EGG?????
			{% if current_user.is_authenticated %}
			{{current_user.username}} is want to know for sure!
			{% endif %}
		</h1>
		<hr>
		{% with messages = get_flashed_messages() %}
		  {% if messages %}
			<ul class="flashes">
				{% for message in messages %}
				<li>{{ message }}</li>
				{% endfor %}
			{% endif %}
		{% endwith %}


		{% block content %}

		{% endblock %}
			<p>
			 <form action="{{ url_for('game') }}">
				<button type="submit">Go</button>
			 </form>
			</p>
	</body>
</html>
